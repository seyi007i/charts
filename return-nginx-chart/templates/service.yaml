apiVersion: v1
kind: Service
metadata:
  name: {{ include "return-nginx-chart.fullname" . }}
  labels:
    {{- include "return-nginx-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "return-nginx-chart.selectorLabels" . | nindent 4 }}
